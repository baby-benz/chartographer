service:
  http:
    unavailable:
      retry-after: 10
  image:
    type: BMP
    parent-path: ./chartas
    lock:
      trying-time: 10

server:
  port: 8080

spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher